import styled from "@emotion/styled";
import colors from "../../styles/color";

export const HandPhoneButton = styled.button`
background-color: ${colors.primary01};
color:${colors.white};
border:none;
padding: 15px 30px 15px 30px;
font-size: 13px;
letter-spacing: 1px;
font-weight: bold;  
cursor: pointer;
`